let fiszero x = (x = 0.0)
let fispos x = (x > 0.0)
let fisneg x = (x < 0.0)
let fneg x = -.x
let fabs x = if x > 0.0 then x else -.x
let fless x y = (x < y)
let fhalf x = x *. 0.5
(*
let fsqr x =
let sqrt x =
let int_of_float x =?
let float_of_int x =?
let sin x =
let cos x =
let atan x =
let floor x =
*)